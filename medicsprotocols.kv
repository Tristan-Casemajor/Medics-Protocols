#:import menu menu
#:import boxlayout_actionbar boxlayout_actionbar
#:import add_mindmap add_mindmap
#:import add_sheet add_sheet
#:import select_file select_file


<MainScreenManager>:
    Screen:
        name: "Menu"
        Menu:

    ScreenObjectProperty:
        layout: add_sheet_layout
        name: "AddSheet"
        BoxLayoutActionBar:
            AddSheetLayout:
                id: add_sheet_layout

    ScreenObjectProperty:
        layout: add_mind_map_layout
        name: "AddMindMap"
        BoxLayoutActionBar:
            AddMindMapLayout:
                id: add_mind_map_layout

    Screen:
        name: "SelectFile"
        BoxLayoutActionBar:
            SelectFileLayout: